rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all application collections in the GST Portal
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
